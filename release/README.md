# FUM 插件安装说明

这是一个“解压即用”的 FUM 插件文件夹。

### 安装步骤：
1. 将此文件夹（`release`）解压。
2. 在 Blender 中进入 `Edit -> Preferences -> Add-ons`。
3. 点击 `Install`，选择此文件夹内的 `__init__.py` 或者直接将整个解压后的文件夹放入 Blender 的插件目录。
4. 勾选启用 `FUM`。

**注意：** 如果你是通过 GitHub 的 `Download ZIP` 下载的，请确保你安装的是解压后包含 `__init__.py` 的那一层文件夹。
